# @package _global_

seed: 12345

defaults:
  - override /trainer: ddp.yaml
  - override /model: ddpm.yaml
  - override /datamodule: mvtec_bottle.yaml

model:
  log_image_period: 50

datamodule:
  batch_size: 48

logger:
  job_type: train

trainer:
  max_epochs: 5000
  min_epochs: 1000
  check_val_every_n_epoch: 10
  resume_from_checkpoint: ${work_dir}/logs/runs/2021-11-24/17-08-34/checkpoints/sample-epoch=2099.ckpt
