# @package _global_

seed: 12345

defaults:
  - override /trainer: ddp4.yaml
  - override /model: SBM/sbm_ncsnpp.yaml
  - override /datamodule: mvtec_bottle.yaml

experiment_name: sbm_ncsnpp

logger:
  job_type: train

