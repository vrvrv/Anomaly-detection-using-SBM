# @package _global_

seed: 12345

defaults:
  - override /trainer: ddp4.yaml
  - override /model: sbm_ddpm.yaml
  - override /datamodule: mvtec_cable.yaml

experiment_name: sbm_ddpm

logger:
  wandb:
    name: ${experiment_name}
    job_type: train